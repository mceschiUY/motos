# Copiar a .env (no se commitea) y cambiar la contraseña.
# La usa docker-compose para SQL Server y para la connection string del backend.
# Requisitos de SQL Server: mínimo 8 caracteres con mayúsculas, minúsculas y números.
DB_PASSWORD=Cambiame_Ya_2026
